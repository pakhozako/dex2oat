# ============================================================
# Dex2oat Lock — system.prop
# OPlus dexopt 优化配置
# ============================================================

# ============================================================
# 🟢 安全 / Safe
# 保守策略，适合日常使用
# ============================================================

# 跳过后台 dexopt
pm.dexopt.bg-dexopt=skip

# OTA 后编译策略
pm.dexopt.boot-after-ota=speed-profile

# Mainline 更新后编译策略
pm.dexopt.boot-after-mainline-update=verify

# 安装时编译策略
pm.dexopt.install=speed-profile

# 批量安装编译策略
pm.dexopt.install-bulk=speed-profile

# 批量安装降级包编译策略
pm.dexopt.install-bulk-downgraded=verify

# 批量安装 secondary dex 编译策略
pm.dexopt.install-bulk-secondary=verify

# 批量安装 secondary dex 降级编译策略
pm.dexopt.install-bulk-secondary-downgraded=verify

# 快速安装路径编译策略
pm.dexopt.install-fast=verify

# 开机后编译策略
pm.dexopt.post-boot=verify

# 首次开机编译策略
pm.dexopt.first-boot=verify

# 共享库编译策略
pm.dexopt.shared=speed

# 闲置 App 编译策略
pm.dexopt.inactive=verify

# A/B OTA 编译策略
pm.dexopt.ab-ota=speed-profile

# 手动触发编译策略
pm.dexopt.cmdline=speed-profile

# 禁用闲置降级
pm.dexopt.downgrade_after_inactive_days=9999

# 关闭调试符号生成，减少 dex2oat 开销
dalvik.vm.dex2oat-minidebuginfo=false
dalvik.vm.minidebuginfo=false

# ColorOS 后台编译开关
persist.sys.oplus.bgdex2oat_enabled=false

# ColorOS 编译缓存 miss 触发阈值
persist.sys.feature.compile.re.cache.miss=0

# ColorOS 编译缓存 fmap 大小
persist.sys.feature.compile.re.fmap.size=0

# 禁用启动缓存
persist.device_config.runtime_native.use_app_image_startup_cache=false

# 禁用 iorap 预读（仅 Android 12，13+ 已移除）
persist.device_config.runtime_native_boot.iorap_readahead_enable=false

# 禁用 iorap 追踪（仅 Android 12，13+ 已移除）
persist.device_config.runtime_native_boot.iorap_perfetto_enable=false

# 禁用 ColorOS opex 模块合并触发
oplus.opex.merge=false
oplus.opex.modulemerge=false

# OPlus 编译器服务 (MTK)
persist.oplus.ocompiler=false

# ZygoteC (MTK)
persist.oplus.zygote_c.enable=false

# Zygote ocomp (MTK)
persist.oplus.zygote_ocomp=false

# 禁用运行时 dexopt
persist.device_config.runtime.dexopt.enabled=false

# 禁用运行时后台 dexopt
persist.device_config.runtime.bg_dexopt.enabled=false

# 禁用运行时 profile merge
persist.device_config.runtime.profile_merge=false

# 禁用 PR dexopt
dalvik.vm.enable_pr_dexopt=false

# 禁用 OTA 异步 PR
dalvik.vm.pr_dexopt_async_for_ota=false

# bg-dexopt 新类阈值
dalvik.vm.bgdexopt.new-classes-percent=0

# bg-dexopt 新方法阈值
dalvik.vm.bgdexopt.new-methods-percent=0


# ============================================================
# 🟡 谨慎 / Caution
# 在安全基础上叠加更多选项，默认禁用，按需启用
# ============================================================

# 全局默认 dex2oat 编译过滤器
# dalvik.vm.dex2oat-filter=speed-profile

# 超大 APK 防降级
# dalvik.vm.dex2oat-very-large=everything

# 禁用启动字符串预解析
# dalvik.vm.dex2oat-resolve-startup-strings=false

# 禁用 ColorOS 温控编译触发
# oplus.dex.tempcontrol=false

# ColorOS dalvik 周期同步任务
# sys.oplus.dalvik_sync_config=0,0,0,0,0

# 禁用 heap 优化触发的 dexopt
# sys.heap.optimize.enable=0
# sys.furtherHeapEnlarge.optimize.enable=0
# sys.gcsupression.optimize.enable=0

# 禁用 secondary dex 自动编译
# dalvik.vm.dexopt.secondary=false

# dexopt 温控截断
# dalvik.vm.dexopt.thermal-cutoff=0

# ART 文件预读缓存
# dalvik.vm.madvise.artfile.size=0

# JIT 代码缓存上限
# dalvik.vm.jitmaxsize=0

# profile 保存间隔设最大值
# dalvik.vm.ps-min-save-period-ms=2147483647

# dex2oat 线程数控制
# system_perf_init.bg-dex2oat-threads=0
# system_perf_init.boot-dex2oat-threads=0
# system_perf_init.dex2oat-threads=0
# dalvik.vm.background-dex2oat-threads=0
# persist.dalvik.vm.dex2oat-threads=0

# dex2oat CPU 亲和性
# dalvik.vm.dex2oat-cpu-set=
# dalvik.vm.boot-dex2oat-cpu-set=
# dalvik.vm.background-dex2oat-cpu-set=
# dalvik.vm.image-dex2oat-cpu-set=

# dex2oat 线程数（短名）
# dalvik.vm.bg-dex2oat-threads=0
# dalvik.vm.image-dex2oat-threads=0
# dalvik.vm.boot-dex2oat-threads=0

# MTK 激进调度
# ro.vendor.dex2oat-aggressive-cpu-set=0,1,2,3,4
# ro.vendor.dex2oat-aggressive-threads=0

# dex2oat 堆大小
# dalvik.vm.dex2oat-Xms=32m
# dalvik.vm.dex2oat-Xmx=256m

# odex/vdex 预读缓存
# dalvik.vm.madvise.odexfile.size=0
# dalvik.vm.madvise.vdexfile.size=0

# profile 首次保存延迟
# dalvik.vm.ps-min-first-save-ms=2147483647


# ============================================================
# 🔴 危险 / Aggressive
# 全量 AOT 性能配置
# ⚠️ 安装耗时增加、发热更高、存储占用增大，仅适合有经验的用户
# ============================================================

# 安装时全量 AOT
# pm.dexopt.install=everything

# OTA 后全量 AOT
# pm.dexopt.boot-after-ota=everything

# 手动编译全量 AOT
# pm.dexopt.cmdline=everything

# 特权 App 全量 AOT
# pm.dexopt.priv-apps-oob=everything

# secondary dex 全量 AOT
# pm.dexopt.secondary=everything

# 全局 dex2oat filter AOT
# dalvik.vm.dex2oat-filter=everything

# 特权 App OOB 列表
# pm.dexopt.priv-apps-oob-list=ALL

# 禁用 ART Service
# dalvik.vm.useartservice=false

# 禁用 JIT
# dalvik.vm.usejit=false
