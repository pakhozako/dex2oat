# Dex2oat Lock 更新日志

## v4.6 (2026-07-03)

### 🔧 Bug 修复

- **[关键] 风险档位切换后重启无效（反复报告根因彻底修复）**：档位切换（安全/谨慎/危险）现在走完整保存链路，同步重新生成 `system.prop` 和 `prop-lock.list`；此前仅更新 `config.json`，导致重启后 service 仍按旧 `system.prop` 应用属性，且下次 WebUI 刷新时 `applySystemPropState` 会把旧属性重新 enable，视觉上呈现档位"被重置"的假象。
- **[关键] 危险模式协议确认后 system.prop 未更新**：接受危险模式协议时与档位切换同修，确保危险档位的规则项目正确写入 `system.prop`。
- **`webui-save.sh` 中 `risk.mode` 状态值来源修正**：从 stage 目录的独立 `risk-mode` 文件改为从已通过校验的 `config.json` 解析，消除两者不一致的窗口。
- **`health-check.sh` 写 `prop-lock.list` 改为原子替换**：原先先截断再追加写入，中断时会留下空或半写的锁文件；现改为临时文件 + `mv -f` 原子替换，与其他写操作保持一致。
- **`skin-unlock.sh` `unlock_locked` 补 `trap` 防临时文件残留**：信号中断时 `VALID_FILE`/`NEXT_FILE` 等临时文件不再泄漏。
- **`skin-unlock.sh` `records_checksum` 修复空文件可绕过完整性校验的问题**：对空或不存在的文件返回固定哨兵值 `EMPTY`，而非空字符串哈希。
- **`redeem-code-verify.sh` curl 增加 `--ssl-reqd`**：兑换码请求现在强制要求 HTTPS 证书验证，不可降级；旧版 curl 不支持该选项时自动 fallback 到无参数模式（并在日志中记录）。去掉 `-L` 重定向跟随，防止 HTTPS 被重定向降级到 HTTP。
- **`redeem-code-verify.sh` `json_escape` 补全制表符转义**：服务端返回字段中含 `\t` 时不再破坏 JSON 结构。
- **三处脚本 `STATE_DIR` 改为可被环境变量覆盖**：`core/prop-lock.sh`、`core/health-check.sh`、`uninstall.sh` 的 `STATE_DIR` 由硬编码改为 `${STATE_DIR:-/data/adb/dex2oat-lock}`，与其他脚本保持一致，方便调试和测试。

## v4.5 (2026-07-02)

- WebUI 自定义配置逻辑重做：规则匹配成功的属性会在首次匹配时默认启用，但不会覆盖用户已经手动保存过的选择。
- 统一前后端默认值语义：布尔项默认关闭，数值上限默认 `9999`，计数项默认 `0`，枚举项默认 `all` / `everything`，避免列表、详情、保存后的 `system.prop` 注释显示不一致。
- 自定义视图现在只展示当前设备实际抓取并命中规则的属性；首次安装或尚未完成匹配时会提示先重新匹配，不再误显示预设全量列表。
- 保留完整规则数据作为元数据来源，展示和写入集合由 `matched-props.txt` 决定，不按厂商或机型预先拆分配置项。
- 收紧 WebUI 写入白名单，只允许写入配置暂存文件、重匹配触发文件和明确的导出目标，降低前端误写 state 目录内部文件的风险。
- 修复全关闭配置无法保存的问题：当用户关闭所有开关时，只有注释项的 `system.prop` 也会被视为合法配置。
- 改进旧配置迁移：旧版 `config.json` 或备份缺少 `explicit` 标记时，不会把历史默认值误判为用户手动配置。
- 完善重新匹配后的刷新流程，防止连续点击触发多个轮询任务，并在后台匹配完成后重新读取匹配结果与配置状态。
- 优化 Monet / Material 3 细节：toast 成功与警告状态使用更清晰的容器色，部分组件 token 回归 `m3-components.css` 分层。
- 重新生成 v4.5 发布包、受保护 WebUI、规则包、完整性基线、manifest 与 SHA256。

## v4.4 (2026-07-01)

- 自动同步版本元数据。

## v4.3 (2026-07-01)

- 自动同步版本元数据。

## v4.2 (2026-06-30)

- 自动同步版本元数据。

## v4.1 (2026-06-30)

### 🎨 WebUI 与移动端体验

- 启动页 Logo、主界面 Logo 与隐藏彩蛋封面更新为本地定制素材，启动文案更新为“可以愛的話 不退縮”。
- 优化启动页到主界面的渐变与 Logo 过渡动画，减少首次进入时图标空白和跳动感。
- 首页顶部状态卡补全 `Dex2oat-Lock` 运行状态说明，用户可直接看到当前是否正常、是否需要重启、是否正在同步或需要处理异常。
- 顶部状态卡的 `system.prop` 查看按钮固定到右上角，并避免与标题、标签和状态说明重叠。
- 首页配置摘要区域补齐为更完整的三卡片信息结构，移除重复的 `dex2oat-lock` 卡片。
- “查看 system.prop”入口调整到顶部卡片右上角，功能区空位由“设备系统配置摘要”展开状态补齐。
- 关于页本地背景色补齐到 8 个，保持 2×4 排版。
- 隐藏彩蛋更新为王杰相关内容：歌词、封面和弹窗元数据会正确显示歌名与歌手，不再把“标题”等字段名显示为正文。

### ☁️ 云端、反馈与管理面板

- 云端/API 继续统一使用 `18082`，用于更新检查、发布信息、模块通信、反馈上传、规则证据和支持者校验。
- 新增 Directus 在线管理面板作为业务数据后台，用于查看反馈、模块通信记录、规则证据、支持者校验记录和支持者相关数据。
- Directus 面板通过 Caddy 与免费 `sslip.io` 域名提供 HTTPS 访问，同时 Directus 服务本身收回到 `127.0.0.1:18083`，避免直接裸露管理端口。
- 服务器侧增加 Directus bridge，`/api/telemetry`、`/api/feedback`、`/api/rule-evidence`、`/api/supporter/verify` 可在校验通过后写入后台集合。
- 支持者纪念包改为必须经过服务器校验后才能启用，无效口令会被拒绝并记录校验结果。
- WebUI 允许当前 `154.219.110.62:18082` HTTP 云端接口，保持 Root 管理器 WebView 环境兼容。

### 🛡 稳定性、安全与发布

- 重新同步 `v4.1 / 410` 版本元数据，更新 `module.prop`、`update.json`、WebUI 元数据和发布文件名。
- 发布包继续只包含模块运行文件、受保护 WebUI 资源和规则包，不包含原始彩蛋歌词、封面、Logo 或未保护源码。
- 重新生成 WebUI 受保护资源、完整性基线、release manifest、source backup 和 SHA256。
- 本轮验证通过 `validate`、完整 `build` 和独立 `release`；最新发布包 SHA256 为 `23b318302c8bbdd275768dc97a55fffc3d99e7c194849c068f86a035d5562bab`。

## Dex2oat-Lock v4.0 更新 🚀

### 🎨 WebUI 体验升级

- 继续打磨手机端 Material Design 3 动画、启动页过渡、卡片密度和操作反馈。
- 首页进一步精简，配置摘要、诊断、安装历史、system.prop 与重匹配入口更清楚。
- 优化支持者计划、反馈入口、Logo 彩蛋和受保护资源加载。

### 🔧 模块逻辑优化

- 强化自定义保存与生成流程，增加防重复提交、失败原因提示和危险项保存确认。
- 优化规则、状态、完整性、云端通信和发布口径，为 4.x 长期维护线打基础。
- 云端 API 统一迁移到 18082，服务器面板职责交给 18080 的 1Panel。

### 🛡 发布与保护

- 更新 WebUI 保护构建链，发布包继续只包含受保护 WebUI 和模块运行文件。
- 重新生成完整性基线、manifest 和 SHA256。

## Dex2oat-Lock v3.9 更新 🚀

### 🎨 WebUI 手机端优化

- 以手机 WebUI 为主重新打磨首页、弹窗和二级窗口。
- 右上角恢复刷新入口，功能中心精简为 2×2 核心操作。
- 优化 Material Design 3 动画、卡片密度和触摸反馈。

### 🔧 模块与云端通信

- 保留可控的模块通信开关，用于更新检查与规则证据同步。
- 精简通信说明，移除公开面板展示。

### 🛠 稳定性

- 修复移动端横向溢出、空图标和异常空值显示。
- 继续强化受保护 WebUI 构建产物与发布流程。

## Dex2oat-Lock v3.8 更新 🚀

### 🎨 WebUI 体验升级

- 优化 Material Design 3 / Material You 视觉质感。
- 启动页 Logo 动画与页面过渡更自然。
- 卡片布局更紧凑，层次更清爽。
- 修复部分页面小卡片排版不协调问题。
- 补全离线图标方案，减少字体依赖。

### ⚙️ 规则库优化

- 规则库更新至 v3.8。
- 优化 everything 匹配逻辑。
- 清理无效、空项和低价值规则。
- 谨慎与进阶规则默认关闭，保持兼容优先。

### 🔧 模块逻辑优化

- 优化状态、完整性与冲突展示。
- 加强配置生成与运行状态同步。
- 改进 WebUI 资源保护与发布包结构。
- 若干稳定性与细节修复。

### 🥚 小彩蛋

- 作者在 WebUI 里继续藏了点小东西，自己去发现吧。

## Dex2oat-Lock v3.7 更新 🚀

### 🎨 WebUI 继续升级

- 优化 Material Design 3 Motion 动画与交互反馈。
- 新增自定义背景图。
- 精简标签与重复文案，修复空值异常显示。

### 🔧 模块逻辑优化

- 修复跨档位保存配置丢失。
- 加强配置保存保护与异常恢复。
- 优化规则库与 everything 匹配逻辑。

### 🥚 小彩蛋

- 作者在 WebUI 里藏了新的小惊喜，自己去发现吧。

## Dex2oat-Lock v3.6 更新 🚀

### 🎨 WebUI 全面升级

- 重构前端架构，遵循 Material Design 3 Motion 规范。
- 组件动画更流畅，页面切换、弹窗、按钮和状态反馈更自然。

### 🔧 模块逻辑优化

- 优化核心流程，提升安装、状态同步和完整性校验稳定性。
- 修复 WebUI 加载、中文编码、诊断保存、首页状态展示等细节问题。

### 🥚 小彩蛋

- 作者在 WebUI 里藏了一个小惊喜，自己去发现吧。

## v3.5 (2026-06-26)

- WebUI 改为 Google / Material 风格，使用系统默认字体，移除外部字体加载。
- 自定义工作台改为搜索、筛选、折叠分组和紧凑选项行，避免一次铺满大量配置项。
- `options.json` 展示文案中文化，并保留系统属性名和值的原始语义以免影响生成逻辑。
- 关于页改为规则数、路径和配置文件等实用信息，GitHub 入口改为整行 `Github项目地址`。
- 风险提示去除可见版本号并更新说明文案，安装输出增加阶段说明、代码提示和等待反馈。
- main 分支与发布版 WebUI 同步为受保护资源，不包含原始 `webroot/js` 和 `webroot/css` 源码目录，版本号更新为 `v3.5 / 350`。

## v3.4.1 (2026-06-26)

- 修复 v3.4 安装过程中卡在“执行冲突检测”的问题：安装期冲突检测增加超时兜底，超时后写入 warning 状态并继续完成安装。
- 重写冲突检测的属性匹配方式，改为按 `key=` 精确匹配，避免正则转义导致误匹配或扫描异常。
- 限制冲突检测扫描模块数量和单文件行数，保留冲突报告与统一状态摘要，避免异常模块拖慢或阻塞安装流程。
- 版本号更新为 `v3.4.1 / 341`，同步发布包、WebUI 元数据和更新接口。

## v3.4 (2026-06-26)

- 修复首装或升级后误报 `Warning (integrity missing)` 的问题：统一 Node baseline 与设备端 shell 校验的 `module.prop` 归一化逻辑，忽略动态 `description` 并消除换行差异。
- 完整性校验新增安全自愈：当发布包形态与旧源码 baseline 不一致时，会对当前安装树重建一次 baseline；核心脚本真正缺失时仍保持 `missing` 告警。
- 安装期清理旧完整性摘要，避免升级安装时旧 `integrity missing` 在新校验完成前继续污染首页和模块管理器状态。
- 修复自定义工作台档位切换后的内容同步表现，当前档位相关配置优先显示。
- 参考 KernelSU 的模块页层级重排 WebUI：首页改为总状态、关注项、健康 / 规则 / 运行三张核心卡、设备摘要和工具入口的结构，整体观感更现代。
- 首页健康状态卡片改为三列布局，并继续优化接近 KernelSU 风格的轻量界面。
- 自定义工作台新增安全 / 谨慎 / 危险档位 tooltip、说明卡和 `docs/RISK_MODES.md` 说明文档，降低误操作风险。
- 自定义工作台新增配置导出 / 备份 / 恢复入口，备份写入 Download，恢复后需手动确认保存再生成 `system.prop`。
- 安装界面新增发布级视觉输出：启动横幅、阶段标签和 20 格进度条，安装体验更清晰。
- 发布脚本更新到 v3.4，输出受保护发布包、未加密包和源码包，并继续禁止发布包携带原始 WebUI 源码。

## v3.3 (2026-06-25)

- 修复发布版规则匹配数量可能始终为 0 的问题，发布保护流程保留 `options.json` 的可解析结构。
- 修复自定义工作台安全 / 谨慎 / 危险档位切换后列表不刷新的问题。
- 优化首页与自定义页布局，保留风险提示只读查看逻辑。
- 模块管理器 `description` 显示 🟩 / 🟨 / 🟥 状态提醒。

## v3.2 (2026-06-25)

- 基于 6 份真机抓取日志补全规则库。
- 统一 `state.prop` 作为安装、匹配、配置、apply、健康、冲突、完整性和 summary 的主状态源。
- 首页、关于、自定义、诊断围绕统一状态重构。
- 发布包 WebUI 使用受保护资源，不包含原始 `webroot/js` 和 `webroot/css` 源码目录。
